{
  "name": "planning-events",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest",
    "test-ui": "vitest --ui",
    "server": "json-server --watch db.json"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.1",
    "json-server": "^0.17.4",
    "vitest": "^4.1.2"
  }
}
